export interface Course {
    id: number,
    title: string,
    creation_date: Date,
    duration: Date,
    description: string
}

export interface User {
    id: number,
    first_name: string,
    last_name: string
}